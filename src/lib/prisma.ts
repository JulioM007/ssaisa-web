// Re-exporta tipos de Prisma para uso conveniente en el proyecto
export { PrismaClient, Prisma } from "@prisma/client"
export type {
  Property,
  PropertyImage,
  PropertyType,
  OperationType,
  PropertyStatus,
  Category,
  Development,
  BlogPost,
  Contact,
  User,
  UserRole,
  InterestType,
  PreferredChannel,
} from "@prisma/client"
